<div class="contents">
  <h1>Awesome Elixir</h1>
  <div class="links_nav">
    <a href="/">All</a>
    <a href="/?min_stars=10">&ge;10⭐</a>
    <a href="/?min_stars=50">&ge;50⭐</a>
    <a href="/?min_stars=100">&ge;100⭐</a>
    <a href="/?min_stars=500">&ge;500⭐</a>
    <a href="/?min_stars=1000">&ge;1000🌟</a>
  </div>

  <p>
    Elixir is amazing. And it has a big awesome list: 
    <%= link "h4cc / awesome-elixir", to: "https://github.com/h4cc/awesome-elixir" %>.
    This is a mirror in which each library is marked by the number of stars in
    <%= link "Github", to: "https://github.com/" %> and the number of days since the last commit.
  </p>
  <h2>Contents</h2>
  <%= for title <- @titles do %>
    <li>
      <%= link title, to: "#" <> get_id(title) %>
    </li>
  <% end %>

  <%= for d <- @data do %>
    <% [name, description, libs] = d %>
    <h2 id="<%= get_id(name) %>"><%= name %></h2>
    <p><%= description %></p>
    <ul>
      <%= for lib <- libs do %>
        <% [lib_name, lib_desc, stars, last_commit] = lib %>
        <li>
          <%= link lib_name(lib_name), to: github_path(lib_name) %>
          ⭐<sub><%= stars %></sub>
          📅<sub><%= last_commit %></sub>
          —
          <span><%= lib_desc %></span>      
        </li>
      <% end %>
    </ul>
  <% end %>
</div>